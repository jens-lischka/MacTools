/* Function-file entry point. The add-in currently exposes only the task pane
 * (declared in the manifest), so no function commands are registered yet.
 * This file exists so the manifest's FunctionFile reference resolves. */

Office.onReady(() => {
  // Intentionally empty — reserved for future ribbon function commands.
});
