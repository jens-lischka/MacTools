/* Function file referenced by the manifest. The ribbon button uses a
 * ShowTaskpane action, so there are no command handlers to register here —
 * the file exists only because the manifest requires a FunctionFile. */

Office.onReady(() => undefined);
